<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">📰 NYT Scraper</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link" href="/">Home</a>
            <a class="nav-item nav-link active" href="/saved">Saved Articles <span class="sr-only">(current)</span></a>
            {{!-- <button class="nav-item btn btn-info scrape-latest">SCRAPE LATEST ARTICLES</button>
            <button class="nav-item btn clear">CLEAR ARTICLES</button> --}}
        </div>
    </div>
</nav>
<br>

<div class="container-fluid">
    <div class="row">
        <div class="col-12 text-center">
            <h1>🗞Saved Articles🗞</h1>
        </div>
    </div>
    <hr>
    <br>

    {{#each articles}}
        {{#if saved}}
            <div class="card">
                <h5 class="card-header">
                    <a href={{link}}>
                        {{title}}
                    </a>
                    <button class="btn btn-danger unsave float-right" data-id="{{_id}}">
                        Unsave Article
                    </button>
                    {{!-- <button class="btn btn-warning note float-right" data-id="{{_id}}">
                        Notes
                    </button> --}}

                </h5>

                <div class="card-body">
                    <p>{{summary}}</p>
                    <button class="btn btn-warning note float-right" data-id="{{_id}}">
                        Notes
                    </button>
                    
                </div>

            </div>
            <br>
        {{/if}}
    {{/each}}

</div>

<div id="modalBox" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Notes</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="container-fluid">
                    <div class="row">
                        {{!-- <div id="modalBody" class="col-12 justify-content-center text-center flex-wrap d-flex"> --}}
                        <div id="modalBody" class="col-12">
                            {{!-- notes go here --}}
                        </div>
                        <form>
                            <div class="form-group">
                                {{!-- <label for="note-input">Enter a note:</label> --}}
                                {{!-- <input type="text" class="form-control" id="note-input" rows="4" placeholder="New Note"> --}}
                                <textarea id="note-input" class="form-control" rows="4" placeholder="New Note"></textarea>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-success" id="save-note">Save Note</button>
            </div>
        </div>
    </div>
</div>

